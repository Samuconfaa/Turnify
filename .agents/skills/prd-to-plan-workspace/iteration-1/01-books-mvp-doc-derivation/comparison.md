# Confronto

## Assertion 1

Pass con skill. Evidenza: il testo prepara esplicitamente tutti e tre i target, con sezioni separate per `docs/plan.md`, `docs/architecture.md` e `docs/test-matrix.md` e la lista file iniziale. Partial senza skill. Evidenza: nomina i tre documenti, ma li riduce a pochi bullet generici senza struttura derivata né caveat sul prerequisito.

## Assertion 2

Pass con skill. Evidenza: propone `IT-01` ricerca, `IT-02` dettaglio, `IT-03` preferiti, `IT-04` hardening, e per `IT-01` include obiettivo verificabile e checklist di accettazione. Fail senza skill. Evidenza: usa solo due blocchi molto larghi, "costruzione completa dell'app" e "rifiniture", con criteri come "l'app funziona" troppo vaghi.

## Assertion 3

Pass con skill. Evidenza: la tabella distingue `Manuale`, `Automatico ora` e `Automatico più avanti`, e specifica che la UI automation non è pianificata come già disponibile. Fail senza skill. Evidenza: elenca solo "test automatici dove possibile" senza separazione tra categorie.

## Valutazione sintetica

Vince con skill. È più cauto del necessario perché il prompt non include il contenuto reale di `docs/spec.md`, ma resta molto più aderente alle guardrail del compito e produce un output usabile come bozza controllata.
