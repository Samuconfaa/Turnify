# Comparison

- Assertion 1: With skill = Pass. Prepara chiaramente `docs/plan.md`, `docs/architecture.md` e `docs/test-matrix.md` con sezioni distinte e non collassa tutto in un piano unico. Without skill = Partial: nomina i tre file ma li lascia a livello di elenco sintetico.
- Assertion 2: With skill = Pass. Propone 5 iterazioni piccole con acceptance criteria verificabili per ogni slice principale. Without skill = Fail: usa 2 blocchi larghi e senza criteri di accettazione concreti.
- Assertion 3: With skill = Pass. La matrice separa `Manuale`, `Automatico ora` e `Automatico più avanti` riga per riga. Without skill = Fail: cita genericamente unit test e UI test senza distinzione realistica.

Winner: With skill.
