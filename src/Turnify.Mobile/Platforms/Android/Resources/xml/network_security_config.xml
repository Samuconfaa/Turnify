<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">samuconfa.it</domain>
        <pin-set expiration="2027-04-28">
            <!--
                SHA-256 fingerprint del certificato pubblico di samuconfa.it.
                Per ottenere il pin aggiornato esegui:
                  openssl s_client -connect samuconfa.it:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform DER | openssl dgst -sha256 -binary | base64
                Aggiorna anche l'expiration e il backup pin prima della scadenza.
            -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <!-- Backup pin (intermediate CA o pin di riserva) -->
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
