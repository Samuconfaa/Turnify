<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Turnify.Mobile.ViewModels"
             x:Class="Turnify.Mobile.Views.AvailabilityPage"
             x:DataType="vm:AvailabilityViewModel"
             Title="{Binding Title}"
             BackgroundColor="{AppThemeBinding Light={StaticResource Background}, Dark={StaticResource DarkBackground}}">

    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20,20,20,40">

            <Label Text="Disponibilità settimanale"
                   FontSize="22" FontAttributes="Bold" FontFamily="PJSXBold"
                   TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}"
                   CharacterSpacing="-0.5" Margin="0,0,0,4"/>
            <Label Text="Seleziona i giorni in cui sei disponibile a ricevere turni."
                   FontSize="13" FontFamily="PJSReg"
                   TextColor="{AppThemeBinding Light={StaticResource OnSurfaceVariant}, Dark={StaticResource DarkOnSurfaceVariant}}"
                   Margin="0,0,0,14"/>

            <!-- Grouped in a single card -->
            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}"
                    Stroke="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" StrokeThickness="1"
                    Padding="0">
                <Border.StrokeShape><RoundRectangle CornerRadius="18"/></Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#000000" Offset="0,4" Radius="16" Opacity="0.06"/>
                </Border.Shadow>
                <VerticalStackLayout Spacing="0">

                    <!-- Lunedì -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Lunedì"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Monday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Martedì -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Martedì"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Tuesday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Mercoledì -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Mercoledì"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Wednesday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Giovedì -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Giovedì"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Thursday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Venerdì -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Venerdì"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{AppThemeBinding Light={StaticResource OnBackground}, Dark={StaticResource DarkOnBackground}}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Friday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Sabato -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Sabato"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{StaticResource Warning}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Saturday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>
                    <BoxView HeightRequest="1" Color="{AppThemeBinding Light={StaticResource OutlineVariant}, Dark={StaticResource DarkOutlineVariant}}" Margin="18,0"/>

                    <!-- Domenica -->
                    <Grid ColumnDefinitions="*,Auto" Padding="18,14">
                        <Label Grid.Column="0" Text="Domenica"
                               FontSize="15" FontFamily="PJSSemi"
                               TextColor="{StaticResource Error}" VerticalOptions="Center"/>
                        <Switch Grid.Column="1" IsToggled="{Binding Sunday}"
                                OnColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}" ThumbColor="White"/>
                    </Grid>

                </VerticalStackLayout>
            </Border>

            <Button Text="Salva"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Margin="0,12,0,0"/>

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center" Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource DarkPrimary}}"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
