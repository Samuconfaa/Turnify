<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Turnify.Mobile.Views.EmojiPickerPage"
             Title="Scegli emoji"
             BackgroundColor="{StaticResource Background}">

    <Grid RowDefinitions="Auto,*,Auto" Padding="16">

        <Label Grid.Row="0"
               Text="Tocca un'emoji per selezionarla come foto profilo"
               FontSize="14"
               TextColor="{StaticResource OnSurfaceVariant}"
               HorizontalTextAlignment="Center"
               Margin="0,8,0,16"/>

        <CollectionView Grid.Row="1"
                        x:Name="EmojiGrid"
                        ItemsSource="{Binding AvailableEmojis}"
                        SelectionMode="Single"
                        SelectionChanged="OnEmojiSelected">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="6" HorizontalItemSpacing="8" VerticalItemSpacing="8"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border BackgroundColor="{StaticResource Surface}"
                            Stroke="{StaticResource Outline}"
                            StrokeThickness="1"
                            Padding="4"
                            HeightRequest="56" WidthRequest="56">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Label Text="{Binding .}"
                               FontSize="28"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="2"
                Text="Annulla"
                Clicked="OnCancelClicked"
                BackgroundColor="Transparent"
                TextColor="{StaticResource Primary}"
                BorderColor="{StaticResource Primary}"
                BorderWidth="1"
                CornerRadius="10"
                HeightRequest="48"
                Margin="0,16,0,0"/>
    </Grid>
</ContentPage>
